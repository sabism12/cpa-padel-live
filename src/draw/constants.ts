/** Shared timing for the Live Group Draw presentation. */
export const SPIN_MS = 5000; // wheel spin duration
export const PAIR_MS = 1900; // "pair revealed" hold
export const GROUP_MS = 2300; // "group revealed" hold

/** Tournament format. */
export const TOTAL_PAIRS = 20;
export const GROUP_COUNT = 5;
export const GROUP_SIZE = 4;
